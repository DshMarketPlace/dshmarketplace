# OpenNext copies node_modules into the Worker bundle. pnpm's symlinked store
# does not survive that copy — nested packages arrive with missing files (the
# @libsql/isomorphic-ws "./web.mjs" resolution failure). A hoisted linker gives
# a real, flat tree that copies cleanly.
node-linker=hoisted
