export const directory = {
  baseUrl: "https://dshmarketplace.dev",
  name: "DSH Marketplace",

  // Head term is "deepseek harness plugins" (plural), taken from Google
  // autocomplete rather than guessed. It leads the title, appears in the H1,
  // and opens the first paragraph — the three places density actually counts.
  title: "DeepSeek Harness Plugins — Browse & Install DSH Plugins",
  description:
    "Browse 1,000+ DeepSeek Harness plugins. Every listing shows the install command, the source repo, the licence and what the plugin can reach. Free, no sign-up.",
};
